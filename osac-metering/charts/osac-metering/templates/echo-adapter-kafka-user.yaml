{{- if .Values.echoAdapter.enabled }}
{{- $tp := include "osac-metering.topicPrefix" . }}
apiVersion: kafka.strimzi.io/v1
kind: KafkaUser
metadata:
  name: osac-metering-echo-adapter
  namespace: {{ include "osac-metering.kafkaClusterNamespace" . }}
  labels:
    strimzi.io/cluster: {{ include "osac-metering.kafkaClusterName" . }}
spec:
  authentication:
    type: scram-sha-512
  authorization:
    type: simple
    acls:
      - resource:
          type: topic
          name: {{ if $tp }}{{ $tp }}.{{ end }}osac.metering.
          patternType: prefix
        operations:
          - Read
          - Describe
        host: "*"
      - resource:
          type: group
          name: osac-metering-echo-adapter
          patternType: literal
        operations:
          - Read
        host: "*"
{{- end }}
