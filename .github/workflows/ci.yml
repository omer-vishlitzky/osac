---
name: CI

on:
  pull_request:
    branches: [main]
  merge_group:

concurrency:
  group: ci-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  changes:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    outputs:
      code: ${{ github.event_name != 'pull_request' || steps.filter.outputs.code == 'true' }}
      fulfillment-service: ${{ github.event_name != 'pull_request' || steps.filter.outputs.fulfillment-service == 'true' }}
      osac-operator: ${{ github.event_name != 'pull_request' || steps.filter.outputs.osac-operator == 'true' }}
      osac-aap: ${{ github.event_name != 'pull_request' || steps.filter.outputs.osac-aap == 'true' }}
      charts: ${{ github.event_name != 'pull_request' || steps.filter.outputs.charts == 'true' }}
      generated-code-components: ${{ steps.filter.outputs.changes || '[]' }}
      floating-tag-components: ${{ steps.floating.outputs.changes || '[]' }}
    steps:
      - uses: dorny/paths-filter@7b450fff21473bca461d4b92ce414b9d0420d706  # v4.0.2
        if: github.event_name == 'pull_request'
        id: filter
        with:
          predicate-quantifier: 'every'
          filters: |
            code:
              - '**'
              - '!**/OWNERS'
              - '!**/LICENSE'
              - '!**/*.md'
              - '!**/docs/**'
              - '!**/.claude/**'
              - '!**/.cursor/**'
              - '!**/.gemini/**'
              - '!.design/**'
              - '!.skillsaw.yaml'
              - '!.pre-commit-config.yaml'
            fulfillment-service:
              - 'fulfillment-service/**'
              - '!fulfillment-service/OWNERS'
              - '!fulfillment-service/LICENSE'
              - '!fulfillment-service/**/*.md'
              - '!fulfillment-service/docs/**'
            osac-operator:
              - 'osac-operator/**'
              - '!osac-operator/OWNERS'
              - '!osac-operator/LICENSE'
              - '!osac-operator/**/*.md'
              - '!osac-operator/docs/**'
            osac-aap:
              - 'osac-aap/**'
              - '!osac-aap/OWNERS'
              - '!osac-aap/LICENSE'
              - '!osac-aap/**/*.md'
              - '!osac-aap/docs/**'
            charts:
              - 'fulfillment-service/charts/**/values.yaml'
              - 'osac-operator/api/**'
              - 'osac-operator/charts/**'
              - 'osac-operator/config/crd/bases/**'
              - 'bare-metal-fulfillment-operator/api/**'
              - 'bare-metal-fulfillment-operator/charts/**'
              - 'bare-metal-fulfillment-operator/config/crd/bases/**'
              - 'fulfillment-service/charts/**'
              - 'osac-aap/charts/**'
              - 'osac-csi-driver/charts/**'
              - 'osac-metering/charts/**'
              - 'osac-installer/**'
      - uses: dorny/paths-filter@7b450fff21473bca461d4b92ce414b9d0420d706  # v4.0.2
        if: github.event_name == 'pull_request'
        id: floating
        with:
          filters: |
            fulfillment-service:
              - 'fulfillment-service/charts/**/values.yaml'
            osac-operator:
              - 'osac-operator/charts/**/values.yaml'
            bare-metal-fulfillment-operator:
              - 'bare-metal-fulfillment-operator/charts/**/values.yaml'
            osac-installer:
              - 'osac-installer/charts/**/values.yaml'
            osac-aap:
              - 'osac-aap/charts/**/values.yaml'

  pre-commit:
    uses: ./.github/workflows/pre-commit.yaml
    permissions:
      contents: read
      issues: write

  check-pr:
    needs: changes
    if: needs.changes.outputs.fulfillment-service == 'true'
    uses: ./.github/workflows/check-pull-request.yaml
    permissions:
      contents: read

  check-generated-code:
    needs: changes
    if: needs.changes.outputs.fulfillment-service == 'true' || needs.changes.outputs.osac-operator == 'true'
    uses: ./.github/workflows/check-generated-code.yaml
    permissions:
      contents: read

  check-floating-tags:
    needs: changes
    if: needs.changes.outputs.charts == 'true'
    uses: ./.github/workflows/check-floating-tags.yaml
    permissions:
      contents: read

  unit-tests:
    needs: changes
    if: needs.changes.outputs.code == 'true'
    uses: ./.github/workflows/unit-tests.yml
    permissions:
      contents: read

  integration-tests:
    needs: changes
    if: needs.changes.outputs.code == 'true'
    uses: ./.github/workflows/integration-tests.yml
    permissions:
      contents: read

  helm-lint:
    needs: changes
    if: needs.changes.outputs.charts == 'true'
    uses: ./.github/workflows/helm-lint.yaml
    permissions:
      contents: read

  ansible-lint:
    needs: changes
    if: needs.changes.outputs.osac-aap == 'true'
    uses: ./.github/workflows/ansible-lint.yml

  codeql:
    uses: ./.github/workflows/codeql.yml
    permissions:
      contents: read
      security-events: write
      packages: read
      actions: read

  dependency-review:
    if: github.event_name == 'pull_request'
    uses: ./.github/workflows/dependency-review.yml
    permissions:
      contents: read

  label-gate:
    uses: ./.github/workflows/label-gate.yml
    with:
      caller-event: ${{ github.event_name }}
    permissions:
      contents: read

  e2e-vmaas:
    needs: changes
    if: needs.changes.outputs.code == 'true'
    uses: ./.github/workflows/e2e-vmaas-full-install.yml
    secrets: inherit

  e2e-bmaas:
    needs: changes
    if: needs.changes.outputs.code == 'true'
    uses: ./.github/workflows/e2e-bmaas-full-install.yml
    secrets: inherit

  e2e-caas:
    needs: changes
    if: needs.changes.outputs.code == 'true'
    uses: ./.github/workflows/e2e-caas-full-install.yml
    secrets: inherit

  check:
    if: always()
    needs:
      - changes
      - pre-commit
      - check-pr
      - check-generated-code
      - check-floating-tags
      - unit-tests
      - integration-tests
      - helm-lint
      - ansible-lint
      - codeql
      - dependency-review
      - label-gate
      - e2e-vmaas
      - e2e-bmaas
      - e2e-caas
    runs-on: ubuntu-latest
    steps:
      - name: Verify all checks passed or were skipped
        env:
          NEEDS_JSON: ${{ toJSON(needs) }}
        run: |
          echo "$NEEDS_JSON" | jq -e '
            to_entries | map(select(.value.result == "failure" or .value.result == "cancelled")) | length == 0
          '
