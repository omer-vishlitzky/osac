{{- if .Values.keycloak.enabled }}
# Reads keycloak-client-secrets (created by the resolve-realm-secrets init
# container in this namespace) and writes fulfillment-controller-credentials
# for the operator running in this same namespace.
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: osac-create-creds
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "osac.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "17"
    helm.sh/hook-delete-policy: before-hook-creation
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: osac-create-creds
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "osac.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "17"
    helm.sh/hook-delete-policy: before-hook-creation
rules:
- apiGroups: [""]
  resources: [secrets]
  resourceNames: [keycloak-client-secrets, fulfillment-controller-credentials]
  verbs: [get]
- apiGroups: [""]
  resources: [secrets]
  verbs: [create, patch]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: osac-create-creds
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "osac.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "17"
    helm.sh/hook-delete-policy: before-hook-creation
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: osac-create-creds
subjects:
- kind: ServiceAccount
  name: osac-create-creds
  namespace: {{ .Release.Namespace }}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: osac-create-creds
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "osac.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "17"
    helm.sh/hook-delete-policy: before-hook-creation
spec:
  backoffLimit: 5
  activeDeadlineSeconds: 300
  ttlSecondsAfterFinished: 3600
  template:
    spec:
      restartPolicy: OnFailure
      serviceAccountName: osac-create-creds
      containers:
      - name: create-creds
        image: {{ .Values.cliImage }}
        command: ["/bin/bash", "/scripts/create-controller-credentials.sh"]
        env:
        - name: HOME
          value: /tmp
        - name: KEYCLOAK_NAMESPACE
          value: {{ .Release.Namespace }}
        - name: OSAC_NAMESPACE
          value: {{ .Release.Namespace }}
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            cpu: 200m
            memory: 256Mi
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - "ALL"
        volumeMounts:
        - name: scripts
          mountPath: /scripts
          readOnly: true
      volumes:
      - name: scripts
        configMap:
          name: osac-keycloak-hook-scripts
          defaultMode: 0755
{{- end }}
